S-表达式命令设计
运算方式加载文法：
	$WLL0		加载自解释文法规则	($WLL0 ($TRANSLATION ...) ($TRANSLATION ...)) => 
	$TRANSLATION	组装文法转换规则	($TRANSLATION ($RULE ...) ($RULE ...)) => MAP_SYMBOL
	$RULE		组装文法规则		($RULE VARIABLE_SYMBOL COMPACT_SYMBOL) => MAP_SYMBOL
	$VARIABLE	组装文法变量		($VARIABLE CONSTANT_SYMBOL ... CONSTANT_SYMBOL) => VARIABLE_SYMBOL
	$CONSTANT	组装文法常量		($CONSTANT 
	$REMARK		组装文法特殊关键字
直接加载规则字符串：
	$LOAD_TRANSLATIONS 加载文法规则
	$ADD_TRANSLATIONS 添加文法规则
	
	
	
	$LIST		($LIST, A, B,...,Z) = (A, B, ..., Z)
		A B ... Z n $LIST = COMPACT(A B ... Z)
	$APPEND		($APPEND, (A, B), (C, D)) = (A, B, C, D)
		(A B) (C D) $APPEND = (A B C D)
	$CAR		($CAR, (A B)) = A
		(A B) $CAR = A
	$CDR		($CDR, (A B)) = (B)
		(A B) $CDR = (B)
	$ADD		($ADD, 1, 2) = 3
		1 2 $ADD = 3
	$SUB		($SUB, 9, 1) = 8
		9 1 $SUB = 8
	$MUL		($MUL, 1, 2) = 2
		1 2 $MUL = 2
	$DIV		($DIV, 9, 3) = 3
		9 3 $DIV
	
	
	S-表达式的两种等价表达形式：
		($LIST, A, B)
		COMPACT_SYMBOL($LIST A B)
		什么时候求值
		什么时候当做原子符号压栈处理
		取决于求值状态
		
	
