xyz文法二义性冲突的解决：
1.现有系统的first或following集合是用orderlist实现的。因此，系统默认的规则就是ascii编码大小顺序，小的字母对应的规则优先。
该策略是稳定的，和规则的排列先后顺序无关。

2.若first或following集合是用list实现的，则在文法有二义性冲突时，排列在前面的规则优先。

B. xyz 应该添加注释功能，该功能可以应用文法自解释来实现。另外xyz语言应该支持""空字符串常量，该特性需要在内嵌的文法规则中支持。


